apiVersion: v1
kind: Service
metadata:
    name: survey-service
spec:
    type: NodePort
    selector:
        app: survey-app
    ports:
        - port: 80
          targetPort: 80
          nodePort: 30007
          protocol: TCP
